import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    padding: 10,
  },
  emptyText: {
    marginTop: 50,
    textAlign: 'center',
    color: '#aaa',
    fontSize: 16,
  },
});
