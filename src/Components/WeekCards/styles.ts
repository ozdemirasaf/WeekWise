import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  weekCard: {
    padding: 15,
    borderRadius: 13,
    marginBottom : 15
  },

  weekCardTitle: {
    fontSize: 20,
    fontWeight: '500',
    color: '#fff',
  },
});
